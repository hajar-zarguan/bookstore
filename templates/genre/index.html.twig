{% extends 'base.html.twig' %}

{% block title %}Genres
{% endblock %}


{% block body %}


<section class="catagory_section layout_padding">
	<div class="catagory_container">
	  <div class="container ">
		<div class="heading_container heading_center">
		  <h2>
			Genres des livres
		  </h2>
		  <p>
			There are many variations of passages of Lorem Ipsum available, but the majority have suffered alteration
		  </p>
		  
	{% if is_granted('ROLE_ADMIN') %}
	<a class="btn btn-success" href="{{ path('genre_new') }}"><i class="fas fa-plus"></i></a>
	{% endif %}
		</div>


		<div class="row">

			{% for genre in genres %}
		  <div class="col-sm-6 col-md-4 ">
			<div class="box ">
			  <div class="img-box">
				<img src="images/cat4.png" alt="">
			  </div>
			  <div class="detail-box">
				<h5>
					{{ genre.nom }}
				</h5>
				<p>
				  fact that a reader will be distracted by the readable content of a page when looking at its layout.
				  The
				  point of using
				</p>

			

				<a  class="btn btn-info"  style="color: white;" href="{{ path('genre_show', {'id': genre.id}) }}"><i class="far fa-eye"></i></a>
						{% if is_granted('ROLE_ADMIN') %}
				<a  class="btn btn-success" style="color: white;" href="{{ path('genre_edit', {'id': genre.id}) }}"><i class="far fa-edit"></i></a>
							{{ include('genre/_delete_form.html.twig') }}
						{% endif %}
			     </div>



			</div>
		  </div>
		  {% endfor %}





		</div>
	  </div>
	</div>
  </section>

  <!-- end catagory section -->


{% endblock %}
